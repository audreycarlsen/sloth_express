<%= form_for(@category) do |f| %>

  <div class="field">
    <%= f.label :name, "Please enter category name"%><br>
    <%= f.text_field :name %>
  </div>

   <div class="actions">
    <%= f.submit %>
  </div>
<%end%>